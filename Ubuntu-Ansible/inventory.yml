all:
  children:
    ubuntu_workstations:
      hosts:
        workstation1:
          ansible_host: 10.0.0.245
      vars:
        ansible_user: ubuntu
        ansible_connection: ssh
        ansible_become_password: ubuntu
        ansible_ssh_pass: ubuntu
        ansible_port: 22